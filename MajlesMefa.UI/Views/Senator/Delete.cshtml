@using MajlesMefa.UI.Models;
@using MajlesMefa.UI.Views.Senator;
@{
    ViewData["Title"] = "حذف نماینده";
}
@model SenatorVm

@{
    Layout = null;
}

<form asp-controller=@SenatorController.CONTROLLER asp-action="Delete" id="DeleteSenatorForm" method="post">

    <div class="card" style="margin:10px;">
        <div class="card-header">
            <p class="mb-0">
                <b>حذف نماینده - شناسه: @Model.Id</b>
            </p>
        </div>

    <div class="modal-body">

        <div asp-validation-summary="ModelOnly"></div>
            <input asp-for="@Model.UserId" hidden />
        <div class="row"><div class="col-12"><br /></div></div>
        <div class="row">
            <div class="form-group col-6">
                <label asp-for="@Model.Name" class="control-label">نام نماینده</label>
                    <input value="@Model.Name" class="form-control" disabled readonly />
            </div>
                
        </div>
        <div class="row"><div class="col-12"><br /></div></div>
        <div class="row"><div class="col-12"><br /></div></div>
        <div class="row">
            <div class="col-12">
                <div class="alert alert-danger" style="width:90%; margin:auto;">
                    آیا از انجام این عملیات مطمئن هستید؟
                </div>
                <br/>
            </div>
        </div>
       

    </div>
    <div class="modal-footer">

        <button onclick="event.preventDefault();saveAndupdate($(this.form), $(this.form).attr('action'));"
                class="btn btn-danger" perform="action">
            حذف
        </button>
    </div>
    </div>

</form>


